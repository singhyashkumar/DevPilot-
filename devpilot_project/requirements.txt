# Runtime dependencies for the FastAPI dashboard and DevPilot analyzer.
# Install with: py -m pip install -r requirements.txt
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.30.0,<1.0.0
GitPython>=3.1.43,<4.0.0
